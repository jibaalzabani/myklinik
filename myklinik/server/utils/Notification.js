// Contoh dummy function untuk integrasi WA Gateway
exports.sendWhatsAppNotification = (phone, message) => {
    console.log(`Sending WA to ${phone}: ${message}`);
    // Integrasi API (misal Twilio atau Fonnte) disini
}